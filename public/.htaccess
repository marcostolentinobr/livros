# Habilitar o módulo de reescrita de URL do Apache
RewriteEngine On

# Se a requisição não for um arquivo existente (ex: CSS, JS, imagens)
RewriteCond %{REQUEST_FILENAME} !-f

# E se a requisição não for um diretório existente
RewriteCond %{REQUEST_FILENAME} !-d

# Redirecionar todas as requisições para index.php passando a rota como parâmetro
# QSA = Query String Append (preserva parâmetros da URL original)
# L = Last (para a execução de outras regras)
RewriteRule ^(.*)$ index.php?route=$1 [QSA,L]
