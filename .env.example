# ============================================
# ARQUIVO DE CONFIGURACAO DE AMBIENTE (EXEMPLO)
# ============================================
# Este arquivo serve como template para o arquivo .env
# O Docker copia automaticamente este arquivo para .env na primeira inicializacao
# 
# IMPORTANTE: NUNCA commite o arquivo .env no Git (ele contem senhas)
# Sempre use o .env.example como base

# ============================================
# URL DA APLICACAO
# ============================================
# URL base usada para gerar links absolutos na aplicacao
# Exemplo: usado em App::url() para criar URLs completas
BASE_URL=http://localhost:8888

# ============================================
# CONFIGURACOES DO BANCO DE DADOS
# ============================================
# Credenciais para conexao com o banco de dados MySQL/MariaDB
# 
# DB_HOST: Nome do servico do banco no docker-compose.yml
#          Use 'db' quando rodando no Docker
#          Use 'localhost' ou '127.0.0.1' se rodando localmente
DB_HOST=db

# Nome do banco de dados criado automaticamente pelo Docker
DB_DATABASE=livros_db

# Usuario da aplicacao (nao e o root)
DB_USERNAME=livros_user

# Senha do usuario da aplicacao
DB_PASSWORD=livros_pass
